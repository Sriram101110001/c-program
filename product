#include <stdio.h>
int main() {    
    int n1, n2, pro;   
    printf("Enter two integers: ");
    scanf("%d %d", &n1, &n2);
    pro = n1 * n2;      
    printf("%d * %d = %d", n1, n2, pro);
    return 0;
}
